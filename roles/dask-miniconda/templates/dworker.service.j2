[Unit]
Description=dask-worker

[Service]
Restart=always
RestartSec=20
TimeoutStartSec=0

ExecStart=/root/.miniconda/bin/conda run -n dask_env dworker {{ hostvars[groups['master'][0]]['ansible_eth0']['ipv4']['address'] }}:8786 --offline

[Install]
WantedBy=multi-user.target